# Advised mode, with coverage:
bash test/test.sh

# Run a single test module, class or even method:
python -m unittest -v test.test_core
python -m unittest -v test.test_core.TestSync
python -m unittest -v test.test_core.TestSync.test_constructor

# Run all tests:
python -m unittest discover -s test -v
